{% extends 'base.html.twig' %}

{% block title %}Hello LivresController!{% endblock %}

{% block body %}
<h1>Détail de livre {{ livre.id }}</h1>
    <div class="row">
        <div class="col-4">
            <img src="{{ livre.image }}" alt="photo livre">
        </div>
        <div class="col-8">
            <H2>{{ livre.titre }} ({{ livre.prix }} DT) </H2>
            <H3>Editeur: {{ livre.editeur }} </H3>
            <H4>Date Edition {{ livre.dateEdition|date("d-m-Y") }}</H4>
            <p><em>{{ livre.resume }}</em></p>
        </div>
    </div>

{% endblock %}
